export const EmotionSettings = {
    maxSuggestions: 4,
    minCharacters: 3,
    triggerDelay: 150, // ms
    css: {
        suggestionClass: 'emotion-pill',
        activeClass: 'is-active'
    }
};
