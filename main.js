import { VentBridge } from './js/integration/vent-bridge.js';

/**
 * MAIN.JS
 * The Ignition: Starts the application.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Fire up the Bridge
    VentBridge.init();
});
